# -----------------------------------------------------------------------------
# CMS PROVIDER SWITCH
# -----------------------------------------------------------------------------
# 可选: pocketbase | payload
CMS_PROVIDER=pocketbase
PUBLIC_CMS_PROVIDER=pocketbase

# -----------------------------------------------------------------------------
# PAYLOAD CMS (when CMS_PROVIDER=payload)
# -----------------------------------------------------------------------------
# Payload 服务端地址（推荐内网地址）
PAYLOAD_URL=http://127.0.0.1:3000
# Payload 前端可访问地址（用于图片/URL 解析）
PUBLIC_PAYLOAD_URL=http://127.0.0.1:3000
# 可选: Payload API Key / Bearer Token（若你的 Payload 接口未公开）
PAYLOAD_API_KEY=

# -----------------------------------------------------------------------------
# POCKETBASE (CMS & Backend)
# -----------------------------------------------------------------------------
# 远程 PocketBase 实例地址 (必填)
PUBLIC_POCKETBASE_URL=https://example.com

# 管理员凭证: 用于 Server-side 高权限操作 (如查询订单、库存管理)
PB_ADMIN_EMAIL=support@example.com
PB_ADMIN_PASSWORD=your_admin_password_here

# 图片 CDN 地址: 用于加速 R2 存储的图片加载 (可选)
PUBLIC_R2_CDN_URL=

# -----------------------------------------------------------------------------
# STRIPE PAYMENTS (Test Mode)
# -----------------------------------------------------------------------------
# Stripe 公钥: 用于前端支付 Elements 加载
PUBLIC_STRIPE_KEY=pk_test_...

# Stripe 私钥: 用于后端创建 PaymentIntent
STRIPE_SECRET_KEY=sk_test_...

# -----------------------------------------------------------------------------
# N8N WORKFLOW (原子操作 API 认证)
# -----------------------------------------------------------------------------
# n8n 工作流调用原子 API 时使用的密钥 (必须与 n8n ⚙️ Config 中的 WEBHOOK_SECRET 一致)
N8N_WEBHOOK_SECRET=n8n-elementhic-webhook-2026


# =============================================================================
# 📊 统计分析 (Umami / Google Analytics 等)
# =============================================================================
# 配置后，会自动尝试将域名加入 CSP 白名单 (构建时生效)
# 方式 1: 直接粘贴完整的追踪代码 (推荐)
# 例如: <script defer src="https://analytics.example.com/script.js" data-website-id="..."></script>
PUBLIC_ANALYTICS_CODE=
